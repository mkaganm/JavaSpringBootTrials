package utils;

public class ConfigProgramming {
    // * DevTools ==> LiveReload
    // * file ==> settings ==> compiler ==> build project automatically
    // * file ==> advanced settings ==> allow auto-make to start even if developed application is running
}
